# Core requirements -- always needed to run platelet_manuscript_pipeline_v2_FIXED.py
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
openpyxl>=3.1

# Optional -- only needed for the LightGBM and SARIMA rows of Table 2.
# If either package is missing, the corresponding model is skipped with a
# printed warning ("LightGBM is not installed. Skipping LightGBM." /
# "statsmodels is not installed. Skipping SARIMA."); the script still runs
# to completion and produces every other table and figure. Pass
# --no-optional-models to skip both intentionally (e.g. for a faster run)
# without needing to uninstall anything.
lightgbm>=4.0
statsmodels>=0.14
